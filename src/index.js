import ReactDOM from 'react-dom/client';

// JS Imports
import App from './App';

// CSS Imports
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
